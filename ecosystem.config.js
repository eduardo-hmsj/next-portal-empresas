module.exports = {
    apps: [
      {
        name: 'next-portal-empresas', // Your project name
        script: 'yarn', // For this example we're using npm, could also be yarn
        args: 'start -p 3010', // Script to start the Strapi server, `start` by default
      }
    ]
  }
  
  